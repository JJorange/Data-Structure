```c
#include<iostream>
using namespace std;

#if 1
template<class T>
struct BSTreeNode
{
	BSTreeNode<T>* _pLeft;
	BSTreeNode<T>* _pRight;
	T _data;

	BSTreeNode(const T& data)
		:_pLeft(NULL)
		, _pRight(NULL)
		, _data(data)
	{}
};

template<class T>
class BSTree
{
	typedef BSTreeNode<T> Node;
	typedef Node* PNode;
public:
	//构造函数
	BSTree()
		: _pRoot(NULL)
	{}


	//插入结点
	bool Insert(const T& data)
	{
		if (NULL == _pRoot)
		{
			_pRoot = new Node(data);
			return true;
		}

		//按照二叉搜索树的性质 找待插入节点的位置
		PNode pCur = _pRoot;
		PNode pParent = NULL;
		while (pCur)
		{
			if (data < pCur->_data)
			{
				pParent = pCur;
				pCur = pCur->_pLeft;
			}

			else if (data > pCur->_data)
			{
				pParent = pCur;
				pCur = pCur->_pRight;
			}
			else
				return false;
		}

		//插入新结点
		pCur = new Node(data);
		if (data < pParent->_data)
			pParent->_pLeft = pCur;
		else
			pParent->_pRight = pCur;

		return true;
	}

	PNode Find(const T& data)
	{
		PNode pCur = _pRoot;
		while (pCur)
		{
			if (data < pCur>_data)
				pCur = pCur->pLeft;
			else if (data > pCur->_data)
				pCur = pCur->_pRight;
			else
				return pCur;
		}
		return NULL;
	}

	bool Delete(const T& data)
	{
		if (NULL == _proot)
			return false;

		//找待删除的结点
		PNode pCur = NULL;
		PNode pParent = NULL;
		while (pCur)
		{
			if (data == pCur->_data)
				break;
			else if (data < pCur->_data)
			{
				pParent = pCur;
				pCur = pCur->_pLeft;
			}

			else
			{
				pParent = pCur;
				pCur = pCur->_pRight;
			}
		}
		if (NULL == pCur)
			return false;

		//删除结点
		if (NULL == pCur->_pRight)//只有左子树
		{
			if (pCur == _pRoot)
				_pRoot = pCur->_pLeft;
			else
			{
				if (pCur == pParent->_pLeft)
					pParent->_pLeft = pCur->_pLeft;
				else
					pParent->_pRight = pCur->_pLeft;
			}
		}
		else if (NULL == pCur->_pLeft)//只有右子树
		{
			if (pCur == pRoot)
				_pRoot = pCur->_pRight;
			else
			{
				if (pCur == pParent->_pLeft)
					pParent->_pLeft = pCur->_pRight;
				else
					pParent->_pRight = pCur->_pRight;
			}
		}
		else
		{
			PNode pDel = pCur->_pRight;
			pParent = pCur;
			while (pDel->_pLeft)
			{
				pParent = pDel;
				pDel = pDel->_pLeft;
			}

			pCur->_data = pDel->_data;
			if (pDel == pParent->_pLeft)
				pParent->_pLeft = pDel->_pRight;
			else
				pParent->_pRight = pDel->_pRight;

			pCur = pDel;
		}

		delete pCur;
		return true;
	}

	void InOrder()
	{
		cout << "InOrder:";
		_InOrder(_pRoot);
	}

private:
	void _InOrder(PNode pRoot)
	{
		if (pRoot)
		{
			_InOrder(pRoot->_pLeft);
			cout << pRoot->_data << " ";
			_InOrder(pRoot->_pRight);
		}
	}


private:
	PNode _pRoot;

};
#endif

void Test1(int)
{
	int a[] = { 5, 3, 4, 1, 7, 8, 2, 6, 0, 9, };
	BSTree<int> t;
	for (size_t i = 0; i < sizeof(a) / sizeof(a[0]); i++)
		t.Insert(a[i]);

	t.InOrder();
}


int main()
{
	//Test();
	int a[] = { 5, 3, 4, 1, 7, 8, 2, 6, 0, 9, };
	BSTree<int> t;
	for (size_t i = 0; i < sizeof(a) / sizeof(a[0]); i++)
		t.Insert(a[i]);

	t.InOrder();
	return 0;
}
```
