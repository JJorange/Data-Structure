```c++
#include<iostream>
using namespace std;


template<class T>
class Heap
{
	//构造函数
	Heap()
	:haep(NULL)
	{}

	//构造函数
	Heap(T arr[], int sz)
	{
		heap = new T[sz];
		if (heap == NULL)
			cout << "内存分配失败！" << endl;
		for (int i = 0; i < sz; i++)
			heap[i] = arr[i];
		currentSize = sz;
	}

	//建立小堆
	bool MinHeap()
	{
		currentPos = (currentPos - 2) / 2;//最初调整位置：最后分支点
		while (currentPos >= 0)
		{
			siftDown(currentPos, currentSize);d4

		}

	}


private:
	T * heap;//存放堆的数组
	int currentSize;//最小堆中当前元素个数
	int currentPos;//最初调整位置
	int siftDown(int start, int m);//从start到m下滑调整成为最小堆
	int siftup(int start);//从start到0上滑调整成为最小堆
};



int main()
{

	return 0;
}6
```
